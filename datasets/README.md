# Datasets

В репозитории хранятся только компактные вспомогательные файлы:

- `times.tar`
- `weekends_and_holidays.csv`

Большой архив `ip_addresses_sample.tar` нужно скачать отдельно и положить в этот же каталог.

Итоговая структура:

```text
datasets/
├── README.md
├── ip_addresses_sample.tar
├── times.tar
└── weekends_and_holidays.csv
```
